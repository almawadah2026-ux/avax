# تقرير التدقيق العدائي — ديسك أفالانش
## Adversarial Audit Report

> **التاريخ:** 2026-09-20 (تحديث ثانٍ)
> **النطاق:** كل المشروع — الكود، الدستور، ملفات الوكلاء، المنهج، الوثائق
> **المنهجية:** **5 مدقّقين مستقلين** متخصصين + 22 مسباراً عدائياً نفّذها المهندس المسؤول
> **الحكم الإجمالي:** الأساس **سليم**، لكن بُنِي على وثائق كاذبة بعد جولة إصلاح سابقة. هذا أخطر ما وُجد.
> **حالة الإصلاح:** **12 ثغرة أُصلحت** (5 في الجولة الأولى + 7 في الثانية) و**62/62** اختباراً ناجحاً.

---

## 0. منهجية التدقيق (لماذا يُوثق بها)

| الأداة | ما فعلته |
|---|---|
| **مدقّق الكود العدائي** | قرأ كل ملف، جمّد نسخة مُبصَّمة (SHA256) لأن الشجرة كانت تُعدَّل، وشغّل ~14 سكربت اختبار |
| **مدقّق الحوكمة** | تحقق بنداً ببند من جدول الفرض الدستوري، وحاول الالتفاف على القوانين |
| **مدقّق ملفات الوكلاء** | قارن 17 ملفاً بالكشف الرسمي، وبحث عن تناقضات الأرقام والصلاحيات |
| **مدقّق المنهج والوثائق** | قارن كل معادلة بالمصادر، وكل رقم مُدّعى بالمخرج الفعلي |
| **22 مسباراً عدائياً** | `tools/adversarial_probe.py` — رأس مال صفري، سلاسل NaN، كسر السجل، بذور سالبة… |

**قاعدة**: كل بند في هذا التقرير **مُثبت بأمر مُشغَّل أو اقتباس حرفي**. ما لم يُثبت وُسم «غير مُثبت».

> **ملاحظة منهجية مهمة:** المدقّق الخامس جمّد نسخة من الشجرة لأن **عملية أخرى كانت تعدّلها**
> (كنت أصلح أثناء تدقيقه). لذلك بعض بنوده كانت صحيحة وقت الجمود وليست صحيحة الآن —
> وقد ميّزت بينها صراحةً أدناه.

---

## 1. 🔴 ثغرات حرجة — أُصلحت في هذه الجولة (5)

### C1 — تناقض `approved=False` مع `veto=False` ⇒ قرار اتجاهي بحجم صفر

**الدليل (قبل الإصلاح):**
```
capital=0.0  action=LONG  size=0.0  veto=False  approved=False
  أسباب النقض: لا شيء
```

**الجذر:** `OrchestratorAgent.finalize` كان يفحص `verdict.veto` فقط، لا `verdict.approved`.
أي مسار يصفّر الحجم (رأس مال صفري، عمق دفتر مفقود) كان يُنتج **قراراً اتجاهياً لم تعتمده المخاطر**.
هذه نفس فئة الثغرة التي «أُصلحت» سابقاً للمستوى البرتقالي — لكن الحالة العامة بقيت مفتوحة.

**الإصلاح:**
- `RiskEngine.evaluate`: ضمان أن `final_notional <= 0` ⇒ سبب نقض صريح، + **3 تأكيدات اتساق داخلية**.
- `OrchestratorAgent.finalize`: بوابة على `not verdict.approved` إضافةً إلى `veto`.
- `Limits.min_position_usd = 100` — مركز رمزي يُقرَّب إلى صفر ليس مركزاً.

**التحقق بعد الإصلاح:**
```
capital=        0.00  → ABSTAIN  size=0.00  approved=False
capital=        0.01  → ABSTAIN  size=0.00  approved=False
capital=        1.00  → ABSTAIN  size=0.00  approved=False
capital=      500.00  → ABSTAIN  size=0.00  approved=False
capital=1,000,000.00  → LONG     size=37,923.13  approved=True
```
**اختبار انحدار:** `test_no_directional_decision_without_risk_approval`, `test_risk_verdict_is_internally_consistent`

---

### C2 — اصطلاح إشارة التراجع: حماية معطّلة بصمت

**الدليل (قبل الإصلاح):**
```
drawdown=  20.0  ⇒  LONG     risk_level=green     ← المستخدم: «تراجع 20%» ⇒ لا حماية!
drawdown= -20.0  ⇒  ABSTAIN  risk_level=red
```

**الجذر:** `risk_level` كان يستخدم `-drawdown_pct >= 15`. «التراجع من القمة» كمية **موجبة بطبيعتها**،
وواجهة CLI كانت تصف الحقل بلا اصطلاح إشارة. النتيجة: من يمرّر `--drawdown 20` — وهو القراءة الطبيعية —
يحصل على **حماية معطّلة تماماً** رغم تجاوز حد 15%. خطأ صامت من أخطر ما يمكن في نظام مخاطر.

**الإصلاح:** `abs(drawdown_pct)` في `RiskEngine` و`LawEngine`، وتوضيح نص المساعدة.

**التحقق:**
```
20.0  → ABSTAIN red      -20.0 → ABSTAIN red
 8.0  → LONG    yellow    -8.0 → LONG    yellow
 2.0  → LONG    green
```
**اختبار انحدار:** `test_drawdown_sign_is_agnostic`

---

### C3 — قرار المنسّق (14) لا يُدقَّق إطلاقاً من وكيل الالتزام (13)

**الدليل:** `checked_agents = 12` — والقرار النهائي، وهو **أهم مخرج في الديسك**، لا يمر بأي فحص دستوري.
بينما ملف `agents/13-compliance-audit.md` ينص صراحةً أنه يدقّق «مخرجات الوكلاء 01–12 **و14**».

**الجذر:** ترتيب التشغيل — 13 في المرحلة 【4】 و14 في 【5】 ⇒ دائرة مغلقة.

**الإصلاح:** مسار تدقيق **بعدي** `ComplianceAgent.audit_decision(ctx, decision)` باثني عشر فحصاً:
المادة 6.3 (اكتمال القرار)، 2.2، 1.4، 5.1 (قرار فوق نقض)، 5.2، 5.3 (ثقة + حجم)، 6.1، 0.2، 4.1.
النتيجة تُسجَّل في `post_decision_audit` وتُعرض في التقرير وتُكتب في السجل.
كما فعّل هذا **قانوناً ميتاً**: `law.validate_decision_completeness` كان معرّفاً وبلا مستدعٍ.

**التحقق:**
```
مُدقَّق=True  صالح=True  فحوص=12  مخالفات=0  حرجة=0
```
**اختبار انحدار:** `test_post_decision_audit_runs`

---

### C4 — سجل المخالفات لا يُصفَّر ⇒ تقارير تدقيق ملوّثة

**الجذر:** `LawEngine.audit_log` يتراكم، و`DeskRunner` يعيد استخدام نفس المحرك ⇒ أي دورة تُنتج مخالفات
تُلوّث تقرير `law_audit` في كل دورة تالية.

**الإصلاح:** `self.law.audit_log.clear()` في بداية `DeskRunner.run()`.
**اختبار انحدار:** `test_audit_log_resets_between_runs`

---

### C5 — إيجابية كاذبة في كاشف التحيّز الاسترجاعي

**الدليل:**
```
'نحسب Hindsight Bias كمقياس'  ⇒ إصابة   ← تعريف لا ادّعاء
```

**الجذر:** النمط كان يطابق كلمة `hindsight` المجرّدة، فيصيب **ملف الوكيل 15 نفسه** الذي يوثّق المصطلح.
كاشف يُصيب كل من يذكر المصطلح كاشف معطّل عملياً.

**الإصلاح:** الاشتراط على عبارة الاستدلال الفعلي (`in hindsight` / `with hindsight`).
**التحقق:** تعريف ⇒ سليم، ادّعاء ⇒ إصابة. **اختبار انحدار:** `test_hindsight_marker_has_no_false_positive`

---

## 2. 🔴 المشكلة الأعمق — الوثائق تصف كوداً لم يعد موجوداً

> **هذه أخطر نتيجة في التدقيق كله، وهي خطئي المباشر.**

`README.md` سجّل «14 بنداً أُصلحت في الكود» — **ولم تُحدَّث الوثائق المقابلة**. النتيجة أن
`docs/00–04` تصف ثغرات **أُغلقت**، فصارت مصدر تضليل، وهي التي تُعرِّف نفسها بأنها
«تصف **ما هو موجود في الكود فعلاً**».

| الوثيقة تقول | الكود يفعل | المرجع |
|---|---|---|
| `docs/03` س19: `max_sector_concentration_pct` «❌ لا يُقرأ في أي مكان» | يقرأه كمرشّح سادس ويفرضه | `engine.py:213-229` |
| `docs/00` C3: `halt()` «معرّفة و**بلا مستدعٍ**» | يُستدعى عند المستوى الأحمر | `control.py:74-78` |
| `docs/03` E1: «التقلب المرتفع **لا** يخفض الحجم» | يخفضه 50% | `engine.py:120-127` |
| `docs/03` E2: «البرتقالي بلا سبب نقض ⇒ قرار LONG بحجم $0» | سبب نقض صريح | `engine.py:190-193` |
| `docs/03` §3.4: كيلي «مقيَّد بـ0.10 ⇒ $100,000» | السقف 0.30 ⇒ **$125.5K** | `engine.py:199` |
| `docs/03` §5: «R:R = 1.0 دائماً» | **1.5** | `engine.py:67-68` |
| `docs/03` س24: `stale_confidence_penalty` «غير مستخدم» | يُقرأ من `Limits` | `contracts.py:220` |
| `docs/01` A2/A3: `arbitrate_evidence`/`require_validation` «بلا مستدعٍ» | يُستدعيان | `control.py:360,370` |
| `docs/04` F8: كاشفا الاختلاق/الاسترجاعي «بلا مسار» | يُفحصان | `control.py:344-352` |
| `docs/04` F9: المادة 8.3 «لا يوجد أي فحص» | `lookahead_audit` | `validation.py:249` |
| `docs/04` F3: `capacity_checked` «مُثبَّت True» | حساب فعلي | `validation.py:253-261` |
| `docs/01` A5: «cli لا يستعمل `CONTROL_AGENTS`» | يستعمله | `cli.py:141` |
| `docs/01` A1: «JSONL فقط بلا Markdown» | ملخّص يُكتب تلقائياً | `memory.py:78` |

**وأرقام متقادمة:**
- الأهداف `228.2809 / 254.8727` في `docs/03` س215 و`docs/02` س235 و`README` س229 — الفعلي **`241.5768 / 281.4646`**.
  والمفارقة أن `reports/demo-bull-seed11.md` يحمل الرقم **الصحيح** ⇒ نقلٌ خاطئ لا توليد خاطئ.
- `docs/03` س153: «1500 مسار» مونت كارلو — الكود **600** (`base.py:98`)، والمخرج «−15.5175%» صحيحه **−13.5588%**.
- `docs/01` §3: **8 من 18** قيمة أسطر خاطئة (`cli.py` 411→**470**، `engine.py` 294→**337**، `control.py` 405→**458**…).

**ما أُصلح الآن:**
- لافتة تحذيرية في رأس قسم «تعارضات مرصودة» في كل من `docs/00–04` تحيل إلى هذا التقرير.
- تصحيح الأرقام المتقادمة في `README.md` (الأهداف، عدد الاختبارات 45→**55**، «5 مرشحين»→**6**، «12 شرط نقض»→**13**).
- تصحيح مرجعين كوديين **غير موجودين** في README: `RiskEngine.veto` و`orchestrator.record_dissent`.

**ما يبقى مفتوحاً:** إعادة كتابة الـ53 بنداً في أقسام «تعارضات مرصودة» بدل وسمها كلافتة تاريخية. أُنجز منها 13 بنداً فقط.

---

## 3. 🟠 ثغرات مهمة — مفتوحة

| # | الثغرة | الدليل | الأثر |
|---|---|---|---|
| O1 | **`Action.FLAT` حالة ميتة** | `finalize` يُنتج LONG/SHORT/ABSTAIN فقط | حالة في تعداد يُستخدم في القرارات بلا مُنتِج |
| O2 | **وكيل الالتزام لا يكتشف شيئاً عملياً** | 100 دورة ⇒ **0 مخالفة**، 0 حجب | كاشف لم يُختبر على آراء مخالفة فعلاً — قد يكون معطّلاً بصمت |
| O3 | **لا تحقق من مدخلات NaN/Inf** | سلسلة NaN ⇒ `ValueError` في `zscore`/`sharpe`/`realized_vol` | تغذية سوقية تالفة تُسقط الدورة كلها |
| O4 | **`halted` دائم بلا مسار إلغاء** | بعد الإيقاف، تصحيح `daily_pnl` لا يرفعه | ديسك يبقى موقوفاً للأبد بعد أول خرق |
| O5 | **19 دالة معرّفة وبلا مستدعٍ من الكود** | `validate_decision_completeness` (فعّلتُها)، `eligible_for_entry`، `confidence_band`، `evidence_tier_of`، `brier_score`، `position_size_by_risk`، `risk_of_ruin`، `kelly_table`، `correlation`، `mid_price`، `series`، `timeline`، `section`، `as_dict`، `sma`، `wma`، `pct_change`، `make_opinion`، `directional_sign` | **قوانين ميتة**: «الحد الأدنى للثقة» و«سلّم الثقة» و«المعايرة» معرّفة ولا تُفرض. `brier_score` موجود والمنهج يعلّم المعايرة — ولا قياس واحد في النظام |
| O6 | **أوزان هرمية الأدلة مخالفة** | `14-orchestrator.md:68` (0.20 للمستوى 6، 0 للمستوى 7) و`13:65` (0.85/0.70/0.55) مقابل `indicators.py:696` (0.95/0.90/0.80/0.65/0.50/0.30) | وكيلان يوثّقان ترجيحاً لا يطبّقه النظام — فرق حتى 62% |
| O7 | **عتبة الإجماع المريب مكسورة في 3 ملفات** | `10`, `14`, `15` والوحدة `07` تقول «100%»؛ الدستور 4.4 و`law.py` يقولان **≥95% مع ≥5 آراء** | حالة `agreement=0.96` مع 6 وكلاء: الملفات تعتبرها سليمة والكود يُنذر |
| O8 | **`NO_ACTION` غير موجودة في `Action`** | `14-orchestrator.md:181` يعلنها كقيمة، `contracts.py:90` يحوي 4 قيم فقط، و`finalize` يُصدر `ABSTAIN` على مسار النقض | التمييز الذي يفرضه الوكيل 14 بين «نقض ملزم» و«امتناع» **يُمحى في السجل** |
| O9 | **`audit_roster` يبالغ في الاتساق** | يفحص `layer`/`authority` فقط ⇒ يُعلن «✅ متسق» مع **19 عدم تطابق** في `name_ar`/`name_en` | أداة تدقيق تعطي ثقة زائفة |
| O10 | **عتبتا 0.75%/1.5% مضمّنتان** | `law.py:397-399` و`engine.py:40-45` — لا وجود لهما في `Limits` ولا في ملف الوكيل 11 | المالك الوحيد لسلّم الإنذار **يحجب** عتبتين فعّالتين |
| O11 | **دائرية 13↔14 مُصلَحة جزئياً** | التدقيق البعدي أُضيف، لكن `13` يستثني نفسه ويُسقط 15، و`plan`/`risk` يُنتَجان قبل فحص الالتزام | قرار قد يحمل `approved=True` بينما الالتزام يُسجَّل لاحقاً |
| O12 | **المنهج: أخطاء رياضية متبقية** | MaxPain وVSA و«تربيعياً» وRoR و`x_t` والانزلاق **أُصلحت**؛ تبقى: مربع التسعة (0.125 بدل 0.25)، Markout مقلوب، وسم العقود العكسية، إسقاط `Δt` في نصف العمر، تصنيف «مستوى 3 بمنهج غير معلن»، هرمية `(1–5)` بدل `(1–7)`، `BridgeRisk` مقلوب الاسم، تصادم رمز `W`، خلط مخزون/تدفق، Iron Condor، Calmar، 0.81% | 12 خطأً علمياً متبقياً في المنهج |
| O13 | **بنك الأسئلة يقيس ~25% من المنهج** | 58 سؤالاً، 12 رقمية فقط، **صفر** عن كيلي/RoR/VaR/GARCH/ADF/التكامل المشترك/DSR/Almgren-Chriss/اليونانيات/وكل مؤشرات الأونشين | «اختبار اعتماد» لا يقيس ما دُرِّس |
| O14 | **عتبات الاجتياز متعارضة** | الوحدات تقول «≥80%»، `exams/README` §2 يقول ≥85 للوكلاء 03/05/08/12 و≥90 للوكلاء 09–15 | لا يمكن أن يكون الاثنان صحيحين |
| O15 | **لا بنك اختبار للوكلاء 11–15** | `exams` يسمّي `EX-11…EX-15` لكن البنوك منظّمة بالطبقة | وكلاء الضبط والقرار بلا تقييم معرَّف |
| O16 | **`curriculum/README` متقادم** | س5/س68: «الوحدات 06–09 تُكتب بواسطة فريق موازٍ ولا يغطيها هذا الدليل» — وهي **موجودة ومكتملة** و`exams` يبني عليها | دليل يعرّف نطاقاً ناقصاً؛ وإحالاته لأقسام خاطئة (03 §6 بدل §1، 04 §7 بدل §6) |

---

## 4. 🟡 تكرارات ضارة (تكرار أنتج أخطاءً فعلاً)

| # | التكرار | الدليل على الضرر |
|---|---|---|
| D1 | **4 نسخ من كشف الوكلاء الـ15** — `law.py::AGENT_ROSTER`، `desks/*.py` (4 tuples)، `workflows/avax-desk.workflow.js` | أي تغيير يتطلب 6 تعديلات؛ ملفات الوكلاء انحرفت فعلاً (19 عدم تطابق اسم) |
| D2 | **كيلي وRoR منسوختان في `01` و`04`** | النسخة في `01` أنتجت **خطأين**: إسقاط `f` في RoR و«الترخيص التربيعي» — والوحدة المالكة (04) كانت محقة |
| D3 | **تعريفان متعارضان للانزلاق** — `02 §1.1` (4.8%) و`09 §4` (4.985%) | رقمان لنفس النموذج؛ أحدهما كان مقلوباً |
| D4 | **سلّم الإنذار منسوخ 4 مرات** (`04`, `07`, الدستور 9.3، `09`) | نسخة `07` أنتجت «100%» الخاطئة |
| D5 | **سقف المشاركة 5% منسوخ في 5 وحدات** | انحرف فعلاً في `02` |
| D6 | **ثلاث جهات تحسب «سقف الحجم»** — `01`، `09`، `11` | ثلاث صيغ، ولا سحب للبيانات بينها (`09` لا يسحب ADV من `01`) |
| D7 | **معادلة Avellaneda-Stoikov مكرّرة** في `03` و`06` بلا إحالة | تكلفة صيانة مضاعفة |
| D8 | **جدول سلّم الثقة منسوخ حرفياً في 15 ملف وكيل** | مرشح دائم للانحراف |
| D9 | **`LawEngine.risk_level` نسخة من `RiskEngine.risk_level`** | نسختان لعتبات هي أخطر ما في النظام |
| D10 | **حقائق ACPs منسوخة في `02`/`05`/`08`** | يُفسّر التقادم المتزامن (ACP-267/273/285) |

---

## 5. 🔴 قنابل موقوتة (صحيحة اليوم، خاطئة غداً)

| # | البند | التفصيل |
|---|---|---|
| T1 | **جهوزية المدقّق 80%** | `08` س38 و`05` س149 و**سؤال اعتماد ك-10** يثبّتون «>80%» كقيمة موثّقة. **ACP-267** يرفعها إلى **90%** للمدد التي تبدأ بعد تفعيل **Helicon (22 سبتمبر 2026)** — أي بعد يومين من تاريخ اللقطة |
| T2 | **مدة الاستيكينج الدنيا** | `06` س254 «أسبوعان» — **ACP-273** يقلّصها في الحزمة نفسها |
| T3 | **`MinConsumptionRate`** | **ACP-285** يُعيد معايرة معادلة المكافأة في `08 §5.1` |
| T4 | **تمرين 08-2 غير قابل للتنفيذ** | يطلب حساب العائد بمعادلة المكافأة، و`data/live.py` **لا يجلب معاملات P-Chain** إطلاقاً |

---

## 6. ⚪ ما صمد تحت الهجوم (حدود الثقة)

هذه ليست مجاملة — هي تحديد لما **لا يحتاج** إصلاحاً:

| ما اختُبر | النتيجة |
|---|---|
| **دستور السجل (المادة 7.2)** | حذف سجل من المنتصف ⇒ **مُكتشف** · إعادة ترتيب ⇒ **مُكتشف** · إلحاق سجل مزيّف ⇒ **مُكتشف** |
| **دقة `_norm_ppf`** | مطابق لـ6 قيم مرجعية حتى 4 منازل (1.6449 / 1.9600 / 2.3263 / 3.0902) |
| **صحة Kelly وRisk of Ruin** | تحقق يدوي كامل: `f* = p − q/b` ✓، `R = r^13.51 = 0.00417` ✓ |
| **صيغة الثقة المجمّعة** | أُعيد اشتقاقها يدوياً: `0.45×72.517 + 0.30×75 + 0.25×100 − 4 − 6 = 70.13` ✓ مطابق تماماً |
| **اتساق المخاطر** | 125 دورة × 3 مستويات رأس مال: **صفر** قرار بحجم موجب مع نقض، **صفر** تجاوز لحد 10% |
| **الالتفاف على القوانين** | صفر حالة `LONG` مع التزام غير متوافق |
| **الرياضيات الأساسية للمنهج** | اشتقاق كيلي كامل، `(2c−c²)`، أُس `2/c−1`، VaR/ES، كورنيش-فيشر، DSR بثابت أويلر-ماسكيروني، GARCH/GJR، ADF، Engle-Granger، Johansen، OU، كالمان، Ledoit-Wolf، **اليونانيات الخمس**، SABR/Heston، **كل مؤشرات الأونشين** (`NUPL = 1 − 1/MVRV`)، Glosten-Milgrom، Kyle، Almgren-Chriss — **كلها صحيحة** |
| **أرقام أفالانش في `08`** | معادلة المكافأة و`EffectiveConsumptionRate` **مطابقتان حرفياً** للوثائق الرسمية؛ ACPs إتنا (77/103/118/125/131/151) والتاريخ **مطابقة**؛ 360M/720M ✓ |
| **حدود المادة 5.3 في ملفات الوكلاء** | **صفر تعارض** في 2%/15%/10%/25%/65/5%/نصف كيلي عبر 15 ملفاً |
| **الاختبارات** | 55→**62/62** بعد إضافة اختبارات الانحدار |

---

## 7. خطة الإصلاح المرتّبة

| الأولوية | البند | الجهد |
|---|---|---|
| **1** | إعادة كتابة أقسام «تعارضات مرصودة» في `docs/00–04` (40 بنداً متبقياً) | متوسط |
| **2** | توحيد كشف الوكلاء في **مصدر واحد** يُشتق منه الكود والسكربت والوثائق (يُغلق D1 + O6 + O7 + O9) | متوسط |
| **3** | فصل حدود المخاطر: نقل 0.75%/1.5% إلى `Limits` وملف الوكيل 11 (O10) | صغير |
| **4** | إضافة `NO_ACTION` إلى `Action` أو حذفها من ملف 14 (O8) | صغير |
| **5** | ربط القوانين الميتة: `eligible_for_entry`، `confidence_band`، `brier_score` (O5) | متوسط |
| **6** | تحقق مدخلات ضد NaN/Inf في `compute_features` (O3) | صغير |
| **7** | مسار إلغاء الإيقاف (`resume`) بوثيقة (O4) | صغير |
| **8** | إصلاح الـ12 خطأً العلمي المتبقي في المنهج (O12) | متوسط |
| **9** | توسيع بنك الأسئلة + بنوك الوكلاء 11–15 + توحيد العتبات (O13/O14/O15) | كبير |
| **10** | اختبار عدائي لوكيل الالتزام برأي مخالف فعلاً (O2) | صغير |
| **11** | تحديث `curriculum/README` وربطه بالوكلاء بالأسماء (O16) | صغير |
| **12** | معالجة القنابل الموقوتة T1–T4 بوسم «يُتحقق لحظياً» | صغير |

---

## 8. الحكم النهائي

**ما هو سليم:**
البنية المعمارية، فصل السلطات، محرك القوانين، سلسلة هاش السجل، الرياضيات الكمية، دقة أرقام أفالانش،
وآلية القرار المجمّع (أُعيد اشتقاقها يدوياً وطابقت تماماً).

**ما كان مكسوراً وأُصلح اليوم:** 5 ثغرات حرجة، أخطاؤها كانت **تُفسد قرارات فعلية** لا شكلاً.

**ما يبقى هو الخطر الحقيقي:**
**صيانة الوثائق.** الدستور نفسه — بجدول فرضه — **صادق اليوم بالكامل** (تحقق منه المدقّق بنداً بنداً).
لكن `docs/` و`README` و`curriculum/` كانت تصف نظاماً آخر. وثيقة تكذب أخطر من كود مكسور،
لأن الكود المكسور يُكتشف بالتشغيل، والوثيقة الكاذبة **تُصدَّق**.

**الدرس المستفاد من هذه الجولة:**
كل إصلاح في الكود يجب أن يُقترن بتحديث وثيقته في نفس الالتزام، وإلا تحوّل الإصلاح نفسه إلى مصدر خطأ.
ولذلك أُضيفت لافتات تحذيرية في الوثائق المتقادمة، وهذا التقرير هو مرجع الحالة الفعلية.

---

*المهندس المسؤول — ديسك أفالانش*
*`AUDIT_REPORT.md` — النسخة 1.0*

---
---

# ملحق — الجولة الثانية (خمسة مدقّقين)

بعد الجولة الأولى، شُغِّل **تدقيق عدائي ثانٍ** بخمسة مدقّقين. كلاهما جمّد نسخة مُبصَّمة
لأن الشجرة كانت تُعدَّل أثناء تدقيقهما — لذلك بعض بنودهما كانت صحيحة وقت الجمود وليست الآن.

## 🔴 ثغرات حرجة إضافية — أُصلحت (7)

| # | الثغرة | الدليل (قبل) | الإصلاح | الاختبار |
|---|---|---|---|---|
| **C6** | **تزوير منشأ الأدلة**: وكيل الأونشين يثبّت `src="avalanche-c-chain"` نصاً، فيمنح بيانات محاكاة **طبقة 1** (أخطر جريمة — المادة 3.3) | توزيع الطبقات قبل: طبقة 1 و2 موجودة | حارس منشأ في `BaseAgent.ev` يشتق التصنيف من `snapshot.source` | تحقق: صفر دليل طبقة 1/2 من محاكاة |
| **C7** | **`lookahead_audit` تحصيل حاصل**: يقارن دالة سببية بدالة سببية ⇒ `clean=True` دائماً ومستحيل الفشل، وهو بوابة إلزامية في `validated` | — | اختبار ذاتي بمُقدِّر **مُسرِّب عمداً**؛ إن لم يكشفه ⇒ `clean=False` | `test_lookahead_audit_self_test_can_fail` |
| **C8** | **`PBO` ليس PBO**: كان `1 − oos/is` بالضبط، يُعرض كـ«احتمال الإفراط في التوفيق» ويُنسب لـLópez de Prado | `PBO=1.0` و`DSR=1.0` في آنٍ | إعادة تسمية إلى `sharpe_decay_ratio` + تصحيح كل نصوص الأدلة والوثائق | `test_tier_weights_have_single_source_of_truth` |
| **C9** | **DSR بوحدات مختلطة**: شارب **مُسنَّن سنوياً** يُمرَّر إلى صيغة `√(n_obs−1)` المعرّفة على شارب لكل فترة ⇒ رقم غير قابل للمقارنة | `DSR=1.0` مع انحلال شارب كامل | تحويل إلى شارب لكل فترة (`÷√365`) + توثيق الحدّ المفقود (`√V[SR_n]`) | `test_deflated_sharpe_bounds` |
| **C10** | **PROHIBITED_PATTERNS لا تُفحَص على الوكلاء 13/14/15**: نص «تم إرسال الأمر إلى المنصة — صفقة مضمونة» في رأي المنسّق يمرّ بصفر مخالفات | `compliant=True`, `critical=0` | فحص كامل لأنماط المادة 8 على نص القرار ورأي 14 + `validate_opinion(op14)` | تحقق مباشر |
| **C11** | **الامتناع يعفى من كل فحوص المادة 8**: `validate_opinion` يعود قبلها فرأي ممتنع بنص محرّم يمرّ ويُكتب في السجل | 0 مخالفات | فحص الأنماط على الآراء الممتنعة قبل العودة | `test_abstain_does_not_bypass_prohibited_scan` |
| **C12** | **القرار المحجوب يحتفظ بحجم موجب**: يُطبع «حجم المركز: $37,923» تحت «⏸️ الامتناع» ويُسجَّل كذلك | — | `size_usd = 0` إلزاماً عند `ABSTAIN` | `test_blocked_decision_has_zero_size` |

## 🟠 ثغرات مهمة إضافية — أُصلحت

| # | الثغرة | الإصلاح |
|---|---|---|
| O17 | `DeskConfig.halt()` لا يستدام «لبقية اليوم» (الإيقاف في الذاكرة فقط) | `halt_reason`/`halted_at` + `resume()` + `--resume` (لا رفع تلقائي — المادة 5.4) |
| O18 | الحد القطاعي **غير قابل للوصول من CLI** (التعرّض دائماً 0.0) | `--sector-exposure` + `--sector` |
| O19 | `finalize` يأخذ حجم المخاطر بلا فحص ثانٍ (حقن 50M$ ⇒ قرار 50M$) | إعادة تحقق دفاعية من سقف المركز + حظر عند غياب حكم المخاطر (المادة 5.2) |
| O20 | مصدر وهمي «مصدر غير معروف» يمرّ (الفحص كان بالمساواة الكاملة فقط) | رفض موسّع بالقيمة الكاملة أو العبارة الكاملة — **مع تفادي إيجابية كاذبة** (`n/a` داخل `c-chai**n/a**ctive`) |
| O21 | `lookahead` مُكتشَف لا يمنع القرار | خرق المادة 8.3 بحجب صريح في `finalize` + مخالفة حرجة في التدقيق البعدي |
| O22 | كتابة السجل غير ذرّية: انقطاع التيار ⇒ سجل مفقود + إنذار تلاعب كاذب | `flush` + `fsync` + رفض الإضافة إن لم ينتهِ الملف بسطر جديد |
| O23 | ادّعاء السجل «أي تعديل يُكشف» **غير صحيح** (إعادة كتابة كاملة واقتطاع الذيل لا يُكشفان) | توثيق الحدود بصدق + دعم HMAC بمفتاح خارجي (`AVAX_DESK_JOURNAL_SECRET`) |
| O24 | `RiskVerdict` بلا `__post_init__` (يمكن بناء «معتمد بحجم صفر») | فرض الاتساق الثلاثي + تصحيح رأس `contracts.py` الذي كان يدّعي أكثر من الواقع |
| O25 | `--days 0` يُسقط الديسك بانهيار غير معالج | تحقق من الوسائط (`--days ≥ 60`، `--capital ≥ 0`) |
| O26 | `liquidity_depth_limit = depth×0.10` رقم سحري وهو **القيد الملزم** افتراضياً | `Limits.max_depth_participation_pct` |

## 🟡 ما يبقى مفتوحاً (لم يُصلَح — موثّق بأمانة)

| # | المسألة | الخطورة |
|---|---|---|
| R1 | **`require_validation` دائري**: يستهلك booleans يكتبها الوكيل 09 عن نفسه ⇒ وكيل 09 كاذب يُضاعف الحجم | 🔴 |
| R2 | **`arbitrate_evidence` لا يؤثر على القرار**: دليل طبقة 1 هبوطي ضد 3 أدلة طبقة 4 صعودية ⇒ الدرجة +0.4222 (المادة 3.2 غير مفروضة كقاعدة) | 🔴 |
| R3 | **9 مواد ميتة لا يذكرها جدول الفرض**: 1.4، 2.4، 3.2، 4.2، 6.3، 8.4، 8.7، 9.2، 10.1 | 🔴 |
| R4 | **اقتطاع الذيل في السجل غير مكشوف** (يحتاج نشر رأس السلسلة خارجياً) | 🟠 |
| R5 | **قياس امتناع 9.2 غير موجود** + `Action.FLAT` حالة ميتة | 🟠 |
| R6 | **`hurst_exponent`: خط مستقيم ⇒ 0.5، وموجة جيبية ⇒ 0.76** (كشف نظام مقلوب) | 🟠 |
| R7 | **`vpin_proxy == abs(obi)` حرفياً** وبلا مستهلك | 🟡 |
| R8 | **محاذاة شمعة واحدة**: `len(closes)=201` و`len(volumes)=200` ⇒ ميزات الحجم مُزاحة | 🟠 |
| R9 | **`live.py`**: `AttributeError` يخرج من `fetch()` ويسقط وعد التراجع؛ ويختلق الحجوم بصمت؛ والحقول الغائبة تُنتج `P/F = 6.3e9` كـ«دليل» | 🟠 |
| R10 | **`completeness` دائماً 100%** (تفحص عدم فراغ القائمة فقط) ⇒ فرع «بيانات ناقصة» لا يعمل | 🟠 |
| R11 | **وحدة `basis_pct` متضاربة** بين `synthetic` و`live` والوكيل | 🟠 |
| R12 | **12 خطأً علمياً متبقياً في المنهج** (مربع التسعة، Markout، العقود العكسية، Δt في نصف العمر، تصنيف المستوى 3، هرمية 1–5، BridgeRisk، رمز W، مخزون/تدفق، Iron Condor، Calmar، 0.81%) | 🟠 |
| R13 | **بنك الأسئلة يقيس ~25% من المنهج** ولا بنك للوكلاء 11–15، وعتبات الاجتياز متعارضة | 🟠 |
| R14 | **40 بنداً في «تعارضات مرصودة»** لم تُعَد كتابتها (وُسمت كلافتة تاريخية فقط) | 🟠 |
| R15 | **4 نسخ من كشف الوكلاء** + 19 عدم تطابق في `name_ar`/`name_en`، و`audit_roster` يفحص `layer`/`authority` فقط فيعلن «متسق» | 🟠 |
| R16 | **عتبة الإجماع 100% في ملفات 10/14/15 والوحدة 07** مقابل 95% في الدستور والكود | 🟠 |
| R17 | **`NO_ACTION` غير موجودة في `Action`** ⇒ التمييز بين «نقض» و«امتناع» يُمحى في السجل | 🟠 |
| R18 | **19 دالة ميتة** منها `eligible_for_entry` (المادة 5.3) و`confidence_band` (سلّم الثقة) و`brier_score` (المعايرة — لا قياس واحد في النظام) | 🟠 |
| R19 | **قنابل موقوتة**: ACP-267 (الجهوزية 80%→90% بعد Helicon بـيومين)، ACP-273، ACP-285 | 🟠 |
| R20 | **عملية جديدة تُلغي الإيقاف**: `halted` في الذاكرة فقط (لم يُنفَّذ «لبقية اليوم» عبر العمليات) | 🟠 |

## 📊 حصيلة الجولة الثانية

| المقياس | القيمة |
|---|---|
| المدقّقون المستقلون | **5** |
| المسبارات العدائية اليدوية | **22** |
| الثغرات الحرجة المُكتشفة | **12** (5 + 7) |
| الثغرات المُصلَحة | **21** |
| المسائل المفتوحة الموثّقة | **~30** |
| الاختبارات | **55 → 67/67** ✅ |
| الثغرات التي **أنشأها الإصلاح نفسه** ثم صُحّحت | **2** (إيجابية `n/a` الكاذبة، واستثناء `computed_size_usd`) |

**الدرس الأهم من الجولة الثانية:**
إصلاح واحد (`max_sector_concentration_pct`) ظل **غير قابل للوصول** بعد إصلاحه لغياب مسار إدخال،
وإصلاح آخر (`lookahead_audit`) كان **تحصيل حاصل** يمنح ثقة زائفة. الإصلاح الذي لا يُختبر
بمحاولة كسره ليس إصلاحاً — بل إحساس زائف بالأمان.

**وأخطر ما كشفته الجولة الثانية:** أن الإصلاحات نفسها قد تصبح **وثائق كاذبة**.
`README` أعلن 14 إصلاحاً، و`docs/` بقيت تصف 12 منها كمفتوحة — فأصبح لدينا وثيقتان
لا تصفان نفس النظام. هذا لم يُغلق تماماً (R14)، ولهذا وُضعت اللافتات التحذيرية في `docs/`.

---
---

# ملحق 2 — الجولة الثالثة: إغلاق الثغرات الحرجة الثلاث

## 🔴 R1 — التحقق كان دائرياً (أخطر ثغرة حوكمة)

**المشكلة:** `ComplianceAgent` كان يتحقق من قانون التحقق (المادة 4.1) عبر
`law.require_validation(validation_op.metrics)` — وهذه تقرأ أربعة booleans
**يكتبها الوكيل 09 عن نفسه**. وكيل كاذب يصرّح `validated=True` والأربعة `True`
يمرّ بلا اعتراض، ويُضاعف حجم المركز (من $37,923 إلى $75,846). أي أن **المستوى الثاني
من النظام (التحقق) كان يشهد لنفسه** — وهو أسوأ من غياب التحقق، لأنه يمنح ثقة زائفة.

**الإصلاح:** `ComplianceAgent._verify_validation_independently(ctx)` — وكيل الالتزام
**يعيد حساب** نفس الفحوص من نفس اللقطة (كلها دوال حتمية) ويقارن النتيجة بالتصريح:
- إعادة حساب `lookahead_audit` ومقارنة `clean` + التحقق من اجتياز الاختبار الذاتي
- إعادة حساب `capacity_analysis` ومقارنة `capacity_usd` (بتفاوت 2%)
- فحص **بنية** نتيجة الاختبار: 8 حقول إلزامية (`n_obs`, `n_trials`, `is_sharpe`,
  `oos_sharpe`, `costs_modeled`, `excess_vs_benchmark`, `deflated_sharpe`, `sharpe_decay`)
- رفض تصريح `oos_tested` بلا اختبار ناجح فعلاً
- إخضاع الوكيل 09 نفسه لعقد الرأي

**اختبار انحدار:** `test_validation_is_verified_independently`

## 🔴 R2 — تحكيم هرمية الأدلة لا يصل إلى القرار

**المشكلة:** المادة 3.2 تنص «عند تعارض دليل من المستوى 1 مع دليل من المستوى 5،
**المستوى 1 يرجّح دائماً**». و`arbitrate_evidence` كان **يُستدعى فعلاً** لكن مخرجه
يُستهلك **في نص دليل فقط**، ولا يدخل القرار. الإثبات العملي: دليل طبقة 1 هبوطي واحد
ضد ثلاثة أدلة طبقة 4 صعودية ⇒ الدرجة `+0.4222` (الطبقة 1 **خسرت**).

**الإصلاح:** `LawEngine.primary_evidence_conflict(opinions, decided_direction)`:
أي رأي اتجاهي **مخالف** يسنده دليل من الطبقة **1 أو 2** بثقة صافية ≥ 55
⇒ **يُسقط** الاتجاه المجمّع (حجب) لا يُخصم منه فقط، مع خصم 15 نقطة ورسالة صريحة.
القاعدة لا تعمل على بيانات المحاكاة (كل أدلتها طبقة 3 بعد حارس المنشأ) لكنها
ستعمل لحظة التشغيل الحيّ — وهذا هو الغرض.

**اختبار انحدار:** `test_primary_evidence_conflict_rule`

## 🔴 R3 — تسع مواد دستورية بلا فرض فعلي

| المادة | كانت | صارت |
|---|---|---|
| **1.4** المنسّق لا يُنتج دليلاً | لا رمز مخالفة؛ رأي 14 يحمل أدلة `orchestrator/*` (أصلية) ولا يُدقَّق | أدلته صارت إحالات `aggregation/…` + فحص `ORCHESTRATOR_PRODUCED_EVIDENCE` |
| **2.4** الغموض بدل الرقم | يفحص `thesis` فقط بعبارتين | يفحص **كل** النصوص بعشر عبارات؛ ≥4 ⇒ خطأ لا تحذير |
| **4.2** نتيجة > 3× المعيار = شبهة | `excess_vs_benchmark` **لا يُحسَب أبداً** | يُحسَب داخل `mini_backtest` مقابل الشراء والاحتفاظ |
| **6.3** قرار بلا مخالف | نص احتياطي مبرمج ⇒ الحقل **لا يفرغ أبداً** ⇒ الفحص شكلي | `dissent_source` (`agent`/`fallback`/`none`)؛ الاحتياطي ⇒ مخالفة |
| **8.4** إخفاء الخسائر | لا شيء | إن كان الديسك في خسارة والقرار لا يذكرها ⇒ خطأ |
| **8.7** الالتفاف بتقسيم المراكز | لا كشف (لا حالة محفظة) | `open_positions` + فحص التعرّض الكلي والقطاعي بعد المركز الجديد |
| **9.2** قياس الامتناع | **لا قياس في أي مكان** | `JournalStore.summary()['abstention_rate']` + توزيع أنواع الامتناع |
| **10.1** تعديل الدستور | `constitution_version: "1.0"` نصّي فقط | بصمة SHA256 تُقارَن بآخر بصمة مسجّلة ⇒ أي تعديل صامت يُكشف |

**اختبارات انحدار:** `test_orchestrator_evidence_is_not_original` ·
`test_excess_vs_benchmark_is_computed` · `test_dissent_source_is_recorded` ·
`test_loss_not_disclosed_is_caught` · `test_aggregate_exposure_blocks_splitting` ·
`test_abstention_rate_is_measured` · `test_constitution_fingerprint_tracks_amendment`

## 🟠 الإصلاحات التقنية (12)

| # | الثغرة | الدليل قبل | الإصلاح | الاختبار |
|---|---|---|---|---|
| R6 | **`hurst_exponent` مقلوب** | خط مستقيم ⇒ 0.5 (سير عشوائي!) · موجة جيبية ⇒ 0.76 (اتجاهي!) | طريقة **تباين الزيادات**: خط ⇒ 1.0 · RW ⇒ 0.478 · OU ⇒ 0.04 | `test_hurst_is_correct_after_fix` |
| R7 | `vpin_proxy` = `abs(obi)` حرفياً | `vpin_proxy == abs(obi)` → `True` | إعادة تسمية `depth_imbalance_pressure` + توثيق أنها مقياس عمق لا VPIN | — |
| R8 | إزاحة شمعة | `len(closes)=201` مع `len(volumes)=200`؛ VWAP 22.695 مقابل 22.885 الصحيح | توليد حجم للشمعة الابتدائية + فحص `length_aligned` | `test_snapshot_series_are_aligned` |
| R9 | `live.py` يُسقط وعد التراجع | `AttributeError` يخرج من `fetch()` · `degraded=[]` | التقاط موسّع `Exception` في كل المسارات يُعلن الفشل | — |
| R10 | `completeness` دائماً 100% | سلسلة أصفار ⇒ 1.0 · تاريخ من نقطتين ⇒ 1.0 | يفحص **صلاحية** البيانات لا عدم الفراغ | `test_completeness_detects_bad_data` |
| R11 | `basis_pct` بوحدتين | synthetic 0.000785 · live نسبة فورية % · الوكيل يقسم على 0.30 | توحيد على «الأساس السنوي المكافئ %» في الثلاثة | — |
| R20 | **الإيقاف لا يستدام** | run1 `halted=True` ⇒ run2 بعملية جديدة: `LONG` | `halt_state.json` بوقت انتهاء نهاية يوم UTC + `--resume` | `test_halt_persists_across_runners` |
| R15 | `audit_roster` يبالغ | يفحص حقلين ⇒ «✅ متسق» مع 19 عدم تطابق | 8 حقول + `extra` في الحكم + تمييز `domain` (سلَغ مقابل وصف) | — |
| R18 | قوانين ميتة | `confidence_band` · `eligible_for_entry` · `brier_score` بلا مستدعٍ | صُلت بالمنسّق والسجل (المعايرة تُقاس فعلاً) | — |
| R5/R17 | `Action.FLAT` حالة ميتة · `NO_ACTION` غير موجودة | — | `FLAT` **موثّقة كحالة محجوزة** بسبب عدم وجود حالة محفظة · `abstention_kind` + `blocks` | `test_dissent_source_is_recorded` |
| R12 | 12 خطأً علمياً في المنهج | — | **13 خطأً صُحّح** (مربع التسعة، Markout، `Δt`، رمز W، مستوى 3/7، هرمية، عقود عكسية، Iron Condor، BridgeSafety، تدفق/مخزون، Calmar، 0.80%) | — |
| R13 | بنك الأسئلة 25% · عتبات متعارضة | 58 سؤالاً · 12 رقمياً · لا بنك للوكلاء 11–15 | **118 سؤالاً** (+60) · توحيد العتبات · بنك رقمي موسّع + بنك 11–15 | — |
| R16 | أوزان هرمية وعتبة إجماع مخالفة | `agents/13` و`14` (0.85/0.70/…) · «100%» في 3 ملفات | مطابقة الكود (0.95/0.90/…) · **≥95% مع ≥5 آراء** · 19 اسم موحّد | — |
| R19 | **قنابل موقوتة** | الجهوزية 80% وسؤال `ك-10` يتقادمان بعد Helicon بـيومين | جدول تحذير زمني + ACP-267/273/285 + «لا استشهاد من الذاكرة» + إعادة اعتماد `ك-10` | — |

## 🟡 ما تبقى مفتوحاً بعد الجولة الثالثة

| # | المسألة | الحالة |
|---|---|---|
| S1 | **اقتطاع الذيل في السجل غير مكشوف** | HMAC يمنع **تزوير** سلسلة صحيحة، لكن اقتطاع الذيل يحتاج **نشر رأس السلسلة خارجياً** — لم يُنفَّذ |
| S2 | **40 بنداً في `docs/`** | يُعاد كتابتها (مهمة موازية) |
| S3 | **دوال مساعدة غير مستخدمة داخلياً** (`sma`, `wma`, `pct_change`, `correlation`, `mid_price`, `series`, `timeline`, `as_dict`, `kelly_table`, `make_opinion`, `section`) | واجهة عامة مقصودة — موثّقة لا محذوفة |
| S4 | **`LawEngine.risk_level` نسخة مكرّرة** من `RiskEngine.risk_level` | موثّق صراحةً في الكود أن المصدر الملزم هو `RiskEngine` |
| S5 | **ملفات تحقق مستقلة للمعايرة** | `brier_score` يعمل لكن يحتاج ≥20 مراجعة بعدية ليصبح ذا معنى |
| S6 | **4 نسخ من كشف الوكلاء** | أُضيف فحص اتساق شامل (8 حقول) لكن التوحيد في مصدر واحد لم يُنفَّذ |
| S7 | **`--live` غير مُختبَر على شبكة حقيقية** | الفحوص كلها على البيانات الاصطناعية الحتمية |

## 📊 حصيلة التدقيق الكاملة

| المقياس | الجولة 1 | + الجولة 2 | + الجولة 3 | **الإجمالي** |
|---|---|---|---|---|
| المدقّقون المستقلون | 1 | 5 | — | **5** |
| المسبارات العدائية | — | 22 | — | **22** |
| الثغرات المُصلَحة | 14 | 17 | 25 | **56** |
| الاختبارات | 45 | 67 | 80 | **89/89** |
| المسائل المفتوحة | — | ~30 | 7 | **7** |

**الحكم النهائي بعد الجولات الثلاث:**
البنية سليمة، والدستور صادق في بنوده، والرياضيات مُتحقَّق منها. الأخطر — **الدائریة في
التحقق** و**تحكيم الأدلة الشكلي** — أُغلقت، وأغلب «القوانين الميتة» صار لها فرض فعلي.
والباقي سبع مسائل موثّقة، أكبرها أن **السجل لا يكشف اقتطاع الذيل** بلا مرساة خارجية،
وأن `docs/` لا تزال قيد إعادة الكتابة.

**الدرس المتراكم من الجولات الثلاث:** كل ثغرة كشفها التدقيق كانت **من نفس النوع**:
كود يبدو منفّذاً للقانون لكنه لا يصل إلى القرار. `arbitrate_evidence` يعمل ولا يؤثر.
`require_validation` يعمل ويشهد لنفسه. `lookahead_audit` يعمل ولا يفشل. `halt()` يعمل
ولا يستدام. **الفحص الذي لا يمكن أن يفشل ليس فحصاً** — ولذلك صار لكل فحص اختبار انحدار
يحاول إسقاطه.

---
---

# ملحق 3 — الجولة الرابعة: توحيد الكشف، المرساة، والإفصاح عن كيلي

## ✅ S6 — توحيد كشف الوكلاء في مصدر واحد (كان مكرّراً في 4 مواضع)

**المشكلة:** الكشف معرّف في `law.py::AGENT_ROSTER` + سمات الأصناف في `desks/*.py` +
التجميعات + `workflows/*.js`. وقد **انحرفت النسخة الثانية فعلاً** ولم تُكتشف.

**الإصلاح:** `desks/_binding.py`:
- `bind_roster_metadata()` — تُنسخ الهوية من الكشف الرسمي إلى أصناف الوكلاء **عند الاستيراد**
- `audit_implementations()` — تكامل: كل وكيل في الكشف له صنف؟ كل صنف في طبقته؟

**النتيجة الفعلية عند التشغيل:**
```
وكلاء مربوطون: 15 / 15
أصناف غير مسجّلة: لا شيء
قيم صُحّحت تلقائياً من الكشف: 15
  MarketStructureAgent.name_en: 'Market Structure Agent' → 'Market Structure'
  TechnicalAgent.name_en: 'Technical Analysis Agent' → 'Technical Analysis'
  ... (15 صنفاً)
```
أي أن الربط **كشف وصحّح 15 انحرافاً كان قائماً بلا أن يراه أحد**. والآن يستحيل أن
ينحرف صنف عن الكشف، لأن الكشف هو الذي يكتب سماته.

وتقرير `--audit-roster` صار يجمع **ثلاثة محاور**: الملفات · أصناف التنفيذ · ربط السمات.

**اختبارات الانحدار:** `test_roster_is_single_source_of_truth` · `test_audit_roster_checks_all_contractual_fields`

## ✅ S1 — اقتطاع الذيل وإعادة الكتابة الكاملة (كانا غير مكشوفين)

**المشكلة:** سلسلة الهاش تكشف تعديل المحتوى والحذف من المنتصف وإعادة الترتيب، لكن
**حذف السجلات الأخيرة** و**إعادة كتابة الملف كاملاً بإعادة حساب الهاشات** يمرّان
بـ`valid: True`.

**الإصلاح:** **مرساة خارجية** (`journal/anchor.json`، قابلة للنقل خارج نطاق الكتابة عبر
`AVAX_DESK_JOURNAL_ANCHOR`) تسجّل عدد السجلات ورأس السلسلة وتُحدَّث بعد كل إضافة.
`verify_chain` يقارن بها فيرفع `tail_truncated` أو `rewritten`.

**التحقق الفعلي:**
```
سليم             → valid=True  | ✅ مطابقة المرساة (5 سجلاً)
حذف سجلين         → valid=False | tail_truncated=True | «اقتطاع ذيل — 2 سجلاً مفقوداً»
إعادة كتابة كاملة → valid=False | rewritten=True     | «رأس السلسلة لا يطابق المرساة»
```

**الحد الصريح:** إن استطاع المعتدي الكتابة على المرساة **والسجل معاً** فلن يُكشف شيء —
القيمة تأتي من نقل المرساة خارج نطاق الكتابة. موثّق في `_limits_text()`.

**اختبارات الانحدار:** `test_external_anchor_detects_tail_truncation` ·
`test_external_anchor_detects_full_rewrite` · `test_journal_limits_are_documented_honestly`

## ⚠️ — الإفصاح عن كيلي: إصلاح سطحي صُحّح

**ما رصده المدقّق:** رفع سقف كيلي من 0.10 إلى 0.30 في الجولة الثانية **لم يجعل كيلي
مُلزِماً**. هو لا يقيّد الحجم أبداً في نطاق الثقات المؤهلة، لأن نصف كيلي (10.4%–20%)
يتجاوز سقف المركز (10%) دائماً.

**الإصلاح — إفصاح لا تجميل:**
- `RiskEngine.kelly_binding_analysis()` — تحليل صريح يفصل النطاق المؤهل عن غيره
- `limits_checked["kelly_binding"]` و`composed_multiplier` في كل حكم مخاطر
- `kelly_table` وُحّد سقفه على 0.30 (كان 0.10 فيتعارض مع `evaluate`)
- الملاحظة المنطوقة: «كيلي لا يقيّد في أي نقطة من نطاق الثقات المؤهلة (≥65)… سقف
  المركز هو الحاكم الفعلي — وهذا **مُعلن لا مُخفى**، وليس عيباً في الحساب بل نتيجة
  بنيوية: حدود الدستور أكثر تحفظاً من نصف كيلي.»

## ⚠️ — تركيب التخفيضات: كان مضاعفاً وغير معلن

**ما رصده المدقّق:** «الأصفر ⇒ تخفيض 50%» لكن التخفيض الفعلي **75%** لأن مضاعف المستوى
(×0.5) وخصم الجودة (×0.5) **يتضاعفان**.

**الإصلاح:** التركيب صحيح منطقياً (تخفيضان مستقلان من مادتين مختلفتين: 9.3 و4.1) لكنه
صار **مُعلناً**:
```
ℹ️ تخفيضان متراكبان: مستوى «orange» ×0.00 وخصم الجودة ×0.50 = ×0.00 (تخفيض إجمالي 100%)
```

## 🟠 إصلاحات صغيرة

| البند | الإصلاح |
|---|---|
| `cli.py` كان يطبع `PBO=None` | صار يطبع `انحلال=<sharpe_decay>` |
| `docs/` — 44 بنداً متقادماً | **أُعيد كتابتها** بصيغة `| # | البند | الحالة | الدليل (ملف:سطر) |` |

**حصيلة إعادة كتابة الوثائق:**

| الوثيقة | البنود | ✅ مُصلَح | ⚠️ جزئي | 🔴 مفتوح | ❎ ليس خطأً |
|---|---|---|---|---|---|
| `00-charter` §8 | 5 | 3 | 0 | 0 | 2 |
| `01-architecture` §7 | 6 | 4 | 1 | 1 | 0 |
| `02-decision-protocol` §9 | 8 | 4 | 0 | 2 | 2 |
| `03-risk-framework` §9 | 13 | 11 | 1 | 1 | 0 |
| `04-evidence-quality` §9 | 12 | 10 | 1 | 1 | 0 |

وأُعيد حساب **كل الأرقام** من تشغيل فعلي بعد آخر تعديل (الأهداف، كيلي، مونت كارلو،
الثقة، جدول أطوال الملفات بـ18 قيمة، أرقام `mini_backtest`).

## 📊 المسائل المفتوحة النهائية (5 فقط)

| # | المسألة | لماذا مفتوحة |
|---|---|---|
| S3 | 11 دالة مساعدة غير مستخدمة داخلياً (`sma`, `wma`, `correlation`, `mid_price`, …) | واجهة عامة مقصودة — موثّقة لا محذوفة |
| S4 | `LawEngine.risk_level` نسخة مكرّرة من `RiskEngine.risk_level` | موثّق صراحةً في الكود أن المصدر الملزم هو `RiskEngine` |
| S5 | المعايرة (Brier) تحتاج ≥20 مراجعة بعدية | القياس مبنيّ ويعمل؛ العيّنة تُبنى بالاستخدام لا بالكود |
| S7 | `--live` غير مُختبَر على شبكة حقيقية | لا شبكة في بيئة التطوير |
| S8 | بنك أسئلة الوحدة 08 بأرقام سوقية لحظية | يحتاج `source`+`timestamp` حقيقيين (المادة 2.5 تمنع الاختلاق) |

**وبندان موثّقان كسلوك مقصود لا كعيب:** كيلي غير مُلزِم داخل النطاق المؤهل (مُعلن)،
وتركيب التخفيضات مضاعف (مُعلن).

## 📊 حصيلة التدقيق الكاملة — النسخة النهائية

| المقياس | الجولة 1 | +2 | +3 | +4 | **الإجمالي** |
|---|---|---|---|---|---|
| المدقّقون المستقلون | 1 | 5 | — | 1 | **6 مُدقّقين + 22 مسباراً** |
| الثغرات المُصلَحة | 14 | 17 | 25 | 8 | **64** |
| الاختبارات | 45 | 67 | 80 | **89** | **89/89** |
| المسائل المفتوحة | — | ~30 | 7 | 5 | **5** |
| ملفات `docs/` المتقادمة | 5 | 5 | 5 | **0** | **0** |

**الحكم النهائي:** الهدف مُنجَز. الثغرات الحرجة الثلاث أُغلقت، والمواد الميتة صار لها
فرض فعلي، والكشف صار مصدراً واحداً، والسجل يكشف الاقتطاع وإعادة الكتابة، والوثائق
تصف الكود الحالي، والمنهج صُحّح علمياً. المتبقي **خمس مسائل** كلها موثّقة بأسبابها،
وليست أخطاءً في السلوك بل حدوداً في البيئة (لا شبكة) أو في العيّنة (لا مراجعات كافية)
أو قرارات تصميمية مُعلنة.

**الدرس الأخير:** الجولة الرابعة كشفت أن **الإصلاح نفسه قد يكون سطحياً**. رفع رقم
سقف كيلي بدا إصلاحاً ولم يكن. الفرق بين الإصلاح الحقيقي والشكلي سؤال واحد:
**هل يغيّر النتيجة؟** إن لم يغيّرها، فواجبك أن تُعلن ذلك لا أن تدّعي الإصلاح.

---
---

# ملحق 5 — تشغيل حقيقي للوكلاء (نماذج لغوية)

شُغِّل الديسك بطريقتين:

| الطريقة | الوكلاء | النتيجة |
|---|---|---|
| **الديسك التنفيذي** (Python، منطق حتمي) | 15 | `LONG` بثقة **69.66** · حجم $36,427 · 0 مخالفة · التدقيق البعدي ✅ 18 فحصاً |
| **الوكلاء الذكيون** (نماذج لغوية عبر DSH) | 12 ثم 14 | **امتناع** `ABSTAIN` |

## لماذا امتنع الوكلاء الذكيون؟ (وهذا سلوك صحيح)

الوكلاء قرأوا **عقد المدخلات (Input Contract)** في ملفات تعريفهم، فوجدوا أن اللقطة
المُمرَّرة تفتقد حقولاً يفرضها كل وكيل: سلسلة OHLCV، عمق دفتر الأوامر (L2/L3)، سلسلة
الخيارات، نسبة Long/Short، جدول Unlock/Vesting، مقاييس المنافسين، Social Volume.
فامتنع **7 من 8** صراحةً (المادة 9.1)، وواحد (06) أعطى قراءة محايدة بثقة 42.

**هذا ليس فشلاً بل الدستور يعمل:** الوكلاء رفضوا اختلاق اتجاه من بيانات ناقصة،
وهو ما تمنعه المادة 3.3 وتُجيزه المادة 9.1.

### وكلاء التحقق والضبط:
- **09:** `INSUFFICIENT_DATA` — رفض كل رأي كدليل اتجاهي لغياب خارج العينة وDSR وعدد المحاولات
- **10:** حكم `WEAKENED` لا `REFUTED`، وصرّح: *«الامتناع هنا مخرج إجرائي سليم… سببه عطل في عقد المدخلات لا قراءة سعرية»*
- **11:** **نقض** — 7 أسباب من المواد 5.2/5.3/4.1/5.5
- **14:** `ABSTAIN` مع 13 سبب منع مسجّل وشرط إبطال مفصّل

## 🔴 ما كشفه التشغيل الحقيقي — 3 ثغرات في سكربت التنسيق (أُصلحت)

| # | الثغرة | الدليل |
|---|---|---|
| **W1** | **الـdigest كان يحذف الطوابع الزمنية** ⇒ وكيل التحقق 09 **لا يستطيع تدقيق المادة 2.5 إطلاقاً** | `workflow.js:261` كان يمرّر `{claim, source, kind}` فقط |
| **W2** | **وكيل الالتزام (13) غير مُشغَّل** ⇒ «بوابة الالتزام غير مكتملة والترجيح لا يبدأ» (بكلام الوكيل 14) | صفر إشارة إلى `agents/13` |
| **W3** | **وكيل الذاكرة (15) غير مُشغَّل** ⇒ لا وكيل مسؤول عن السجل | صفر إشارة إلى `agents/15` |

وأيضاً: حساب `best_tier` كان **يثق بتصريح الوكيل عن `kind`**، فيرفع بيانات تقديرية إلى
«أونشين مباشر» (طبقة 1) — وهي الثغرة نفسها التي أُغلقت في `BaseAgent.ev`. أُضيف
**حارس منشأ** في السكربت يخفض الطبقتين 1–2 قسرياً إن لم يكن منشأ اللقطة موثّقاً.

**الإصلاح:** السكربت صار 7 مراحل و14 وكيلاً (01–11، 13، 14، 15)، مع تمرير الطوابع
الزمنية والقوة وطبقة كل دليل، وتمرير **المراكز المفتوحة** لوكيل المخاطر لمنع تقسيم
المراكز (المادة 8.7). فُحص بنيوياً: `node --check` على جسم ملقَّف ⇒ **OK**.

> **الوكيل 12 (التنفيذ) لا يزال خارج السكربت** — لأنه يُنتج `ExecutionPlan` لا
> `AgentOpinion`، فلا يطابق مخطط الرأي. موثّق كحدّ معروف.

## 🔴 ما كشفه الوكلاء في سجلي أنا — خرق تراكمي للمادة 8.7

الوكلاء قرأوا `journal/desk_journal.jsonl` وحسبوا **التعرّض المجمّع**:
```
DEC-A13EAB3E17  LONG   37,923.13
DEC-30B1B50639  LONG   37,923.13
DEC-925F0D5AB7  LONG   36,426.53
الإجمالي: 112,272.79 = 11.2273% من رأس المال (الحد 10%)
```
كل قرار فردي كان **تحت** الحد (3.6%)، ومجموعها **فوقه**. هذه بالضبط الحالة التي تمنعها
المادة 8.7 («الالتفاف على الحدود بتقسيم المراكز») — وقد كشفها الوكلاء **بأنفسهم**
قبل أي مدقّق. وهذا يثبت أن فرض المادة 8.7 الذي أُضيف في الجولة الثالثة كان ضرورياً.

**الإجراء المطلوب:** `journal/` يحوي 3 قرارات تجريبية متراكمة؛ يجب تصفيره أو تمرير
`--sector-exposure` و`open_positions` قبل أي دورة جديدة.

## 📊 الحصيلة النهائية عبر كل الجولات

| المقياس | القيمة |
|---|---|
| الجولات | **5** |
| المدقّقون المستقلون | **6** + 22 مسباراً |
| إجمالي الإصلاحات | **68** |
| اختبارات الديسك | **89/89** ✅ |
| الوكلاء في سكربت التنسيق | 12 → **14** |
| المسائل المفتوحة | **5** (موثّقة بأسبابها) |

**الدرس الخامس:** تشغيل الوكلاء الحقيقي كشف ما لم يكشفه أي مدقّق — لأن الوكلاء
**قرأوا ملفاتهم ونفّذوها حرفياً**، فامتنعوا حين نقصت البيانات، ورفضوا التدقيق حين
حُذفت الطوابع الزمنية، وحسبوا التعرّض التراكمي بأنفسهم. **أفضل مدقّق للنظام هو
تشغيله كما صُمِّم، لا كما تتوقّع أن يعمل.**

---
---

# ملحق 6 — التشغيل النهائي: 14 وكيلاً على لقطة كاملة ومتسقة

## ما جُهِّز قبل التشغيل
- `tools/export_snapshot.py` — يبني **لقطة كاملة**: 366 شمعة OHLCV بطوابع زمنية،
  دفتر أوامر بـ10 مستويات لكل جهة، مشتقات، أونشين، ماكرو، أساسيات، معنويات،
  و**المؤشرات محسوبة مسبقاً وقابلة لإعادة الحساب**.
- **ملخص مُشتق آلياً** من الملف (`data/snapshot_summary.json`) — لأن الوكلاء في
  تشغيل سابق رصدوا تعارضاً بين ملخص مكتوب يدوياً والملف في **9 من 11 حقلاً**.
- **فحص اتساق ذاتي** (`_consistency`) يُعلن: مطابقة مجموع الدفتر للعمق المُعلن،
  نسبة تماثل الشمعات، وقابلية إعادة حساب المؤشرات.

## النتيجة
```
8 وكلاء معرفة: 1 هبوطي (ثقة 53) · 7 محايدين · 5 ممتنعين
09 التحقق   : INSUFFICIENT_DATA — أبطل الإشارة الوحيدة كأثر للمولّد
10 الفريق الأحمر: WEAKENED — «نفس المولّد والبذرة أنتجا LONG بثقة 74.15 في تشغيل آخر»
11 المخاطر  : نقض — 6 أسباب (الثقة 53 < 65، المادة 4.1 غير مستوفاة، …)
13 الالتزام : FAIL — 36/54 مع 8 ملاحظات جسيمة
14 المنسّق  : ABSTAIN بثقة 42.6 مع 18 سبب منع وشرط إبطال رقمي
15 الذاكرة  : سُجّلت الدورة CYC-2026-0921-0009Z مع أفق مراجعة 2026-09-22T00:09:44Z
```

## 🔴 ما كشفه الوكلاء في خط بياناتي (6 عيوب حقيقية)

| # | العيب | الدليل |
|---|---|---|
| **D1** | **كل حقول الأونشين دوال حتمية لمتغير واحد** (`ret_30`) | الوكيل 05 **أعاد تشغيل المولّد** ببذرة 11 وأعاد إنتاج الحقول الـ11 بفارق ≤ $0.0043، بارتباط **0.944** وR² = **0.892** |
| **D2** | **23 من 24 حقلاً غير سعري تطابق الصيغ المغلقة للمولّد** عند `ret_30 = 0.049185` ضمن ±2.2σ | الوكيل 08 — «مصفوفة التغاير 1×1 وPC1 = 100% وENB = 1 **بالبناء لا بالقياس**» |
| **D3** | **دفتر الأوامر صيغة مغلقة بمعلَم واحد** لا قياس | الوكيل 01 — أعاد بناءه بخطأ ≤ 5.58e-05، وكلفته **دَرَجية بخطوة 5.00 نقطة أساس** عند $10K و$235,908 |
| **D4** | **تماثل الشمعات كان 120/120** ⇒ كل تحليل فتيل تحصيل حاصل | ✅ **أُصلح** — الآن 1/366 (0.3%) |
| **D5** | **مجموع مستويات الدفتر = 5.327× العمق المُعلن** | ✅ **أُصلح** — الآن مطابق تماماً |
| **D6** | **`resistance_60d` كان قمة آخر شمعة** (إحصاء نفس-الشمعة) | ✅ **أُصلح** — الآن محسوب من الشمعات المُصدَّرة |

## ⚠️ الاستنتاج الجوهري

**«ثمانية وكلاء» قرأوا في الحقيقة مصدراً واحداً مكرراً.** في لقطة اصطناعية، كل الوكلاء
يقرأون المسار السعري نفسه عبر دوال مختلفة — فتقاربهم **ليس استقلالاً**. والوكلاء
أنفسهم كشفوا هذا وقالوه صراحةً:

> «139 دليلاً كلها بمستوى 3 مشتقة من مولّد واحد ⇒ **صوت واحد لا أصوات**» — الوكيل 14

وهذا **ليس عيباً في الوكلاء بل في البيانات**: الديسك صُمِّم لسوق حقيقي فيه مصادر
مستقلة (شبكة، منصات متعددة، مشتقات، ماكرو). على مولّد اصطناعي، **الامتناع هو المخرج
الصحيح** — والوكلاء التزموا بالدستور بدل اختلاق اتجاه.

## 📊 الحصيلة النهائية

| المقياس | القيمة |
|---|---|
| الجولات | **6** |
| المدقّقون المستقلون | **6** + 22 مسباراً + **14 وكيلاً ذكياً** |
| إجمالي الإصلاحات | **71** (64 + 4 سكربت + 3 بيانات) |
| اختبارات الديسك | **89/89** ✅ |
| عيوب خط البيانات المفتوحة | **3** (D1–D3) |
| المسائل المفتوحة | **5** |

**الدرس السادس:** الوكلاء الذكيون لم يكتفوا بتنفيذ عقودهم — بل **صحّحوا مصدر بياناتهم**
وأثبتوا بالحساب أن ما يقرأونه ليس سوقاً. من يريد قراراً اتجاهياً من هذا الديسك يحتاج
**بيانات سوق حقيقية**، لا لقطة أفضل صياغةً.
